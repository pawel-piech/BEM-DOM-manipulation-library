# BEM-DOM-manipulation-library

Install the dependencies and devDependencies and start the server.

```js
'use strict';

// Setup
const $ = require('bem-query');
const bemQuery = $('block elem');
```
